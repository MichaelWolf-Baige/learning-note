# 12 道题全在这，填空位置已标好

---

# 第 1 题：互斥锁

```c
#include <stdio.h>
#include <unistd.h>
#include "ohos_init.h"
#include "cmsis_os2.h"

// ██████████████████████████████████████
// █  第1题多写：全局变量              █
// ██████████████████████████████████████
static int g_ticket_count = 100;

// ██████████████████████████████████████
// █  第1题多写：辅助函数              █
// ██████████████████████████████████████
osThreadId_t newThread(char *name, osThreadFunc_t func, void *arg)
{
    osThreadAttr_t attr = { name,0,NULL,0,NULL,1024*2,osPriorityNormal,0,0 };
    return osThreadNew(func, arg, &attr);
}

void number_thread(void *arg)
{
    osMutexId_t *mid = (osMutexId_t *)arg;
    while (1) {
        osMutexAcquire(*mid, osWaitForever);          // ← 互斥锁 API
        if (g_ticket_count > 0) {
            g_ticket_count--;
            osMutexRelease(*mid);
            osDelay(200);
        } else {
            osMutexRelease(*mid);
            break;
        }
    }
}

// ██████████████████████████████████████
// █  以下是骨架，每题必写              █
// ██████████████████████████████████████

static void rtosv2_mutex_main_cjh_hys(void *arg)
{
    (void)arg;

    // ██████████████████████████████████████
    // █  第1题多写：初始化              █
    // ██████████████████████████████████████
    osMutexId_t mid = osMutexNew(&(osMutexAttr_t){0});
    newThread("cjh_hys1", number_thread, &mid);
    newThread("cjh_hys2", number_thread, &mid);
    newThread("cjh_hys3", number_thread, &mid);
    osDelay(30000);
}

// ▼▼▼ 以下骨架不变部分 ▼▼▼
static void MutexTestTask(void)
{
    osThreadAttr_t attr;
    attr.name = "rtosv2_mutex_main_cjh_hys";
    attr.stack_size = 4096;
    attr.priority = osPriorityNormal;
    osThreadNew(rtosv2_mutex_main_cjh_hys, NULL, &attr);
}
APP_FEATURE_INIT(MutexTestTask);
```

---

# 第 2 题：信号量

```c
#include <stdio.h>
#include <unistd.h>
#include "ohos_init.h"
#include "cmsis_os2.h"

// ███ 第2题多写：全局 ███
static osSemaphoreId_t semMachine;

// ███ 第2题多写：辅助函数 ███
osThreadId_t newThread(char *name, osThreadFunc_t func, void *arg)
{
    osThreadAttr_t attr = { name,0,NULL,0,NULL,1024*2,osPriorityNormal,0,0 };
    return osThreadNew(func, arg, &attr);
}

void worker_thread(void *arg)
{
    int *work_time = (int *)arg;
    while (1) {
        osSemaphoreAcquire(semMachine, osWaitForever);   // ← 信号量 API
        osDelay(*work_time);
        osSemaphoreRelease(semMachine);
        osDelay(100);
    }
}

// █████████ 骨架 █████████
static void rtosv2_semp_main_cjh_hys(void *arg)
{
    (void)arg;

    // ███ 第2题多写：初始化 ███
    semMachine = osSemaphoreNew(3, 3, NULL);
    int t1=200, t2=300, t3=400, t4=500;
    newThread("cjh_hys1", worker_thread, &t1);
    newThread("cjh_hys2", worker_thread, &t2);
    newThread("cjh_hys3", worker_thread, &t3);
    newThread("cjh_hys4", worker_thread, &t4);
    osDelay(10000);
}

// ▼▼▼ 骨架不变 ▼▼▼
static void SempTestTask(void)
{
    osThreadAttr_t attr;
    attr.name = "rtosv2_semp_main_cjh_hys";
    attr.stack_size = 4096;
    attr.priority = osPriorityNormal;
    osThreadNew(rtosv2_semp_main_cjh_hys, NULL, &attr);
}
APP_FEATURE_INIT(SempTestTask);
```

---

# 第 3 题：队列

```c
#include <stdio.h>
#include <unistd.h>
#include "ohos_init.h"
#include "cmsis_os2.h"

// ███ 第3题多写：全局 ███
typedef struct { char letter; int sender_id; } Message_t;
static osMessageQueueId_t msgQueue;

// ███ 第3题多写：辅助函数 ███
osThreadId_t newThread(char *name, osThreadFunc_t func, void *arg)
{
    osThreadAttr_t attr = { name,0,NULL,0,NULL,1024*2,osPriorityNormal,0,0 };
    return osThreadNew(func, arg, &attr);
}

void sender_thread(void *arg)
{
    int id = *(int *)arg;
    Message_t msg;
    for (char c='a'; c<='z'; c++) {
        msg.letter=c; msg.sender_id=id;
        osMessageQueuePut(msgQueue, &msg, 0, osWaitForever);
        osDelay(100);
    }
}

void receiver_thread(void *arg)
{
    int id = *(int *)arg;
    Message_t msg;
    while (1) {
        osMessageQueueGet(msgQueue, &msg, NULL, osWaitForever);
        if (msg.letter == 'z') break;
    }
}

// █████████ 骨架 █████████
static void rtosv2_msgq_main_cjh_hys(void *arg)
{
    (void)arg;

    // ███ 第3题多写：初始化 ███
    msgQueue = osMessageQueueNew(10, sizeof(Message_t), NULL);
    int r1=1,r2=2,r3=3, s1=1,s2=2;
    newThread("Receiver1",receiver_thread,&r1);
    newThread("Receiver2",receiver_thread,&r2);
    newThread("Receiver3",receiver_thread,&r3);
    newThread("Sender1",sender_thread,&s1);
    newThread("Sender2",sender_thread,&s2);
    osDelay(5000);
    osMessageQueueDelete(msgQueue);
}

// ▼▼▼ 骨架不变 ▼▼▼
static void MessageTestTask(void)
{
    osThreadAttr_t attr;
    attr.name = "rtosv2_msgq_main_cjh_hys";
    attr.stack_size = 4096;
    attr.priority = osPriorityNormal;
    osThreadNew(rtosv2_msgq_main_cjh_hys, NULL, &attr);
}
APP_FEATURE_INIT(MessageTestTask);
```

---

# 第 4 题：按键蜂鸣器

> 头文件加：`#include "iot_gpio.h"` `#include "iot_pwm.h"` `#include "hi_io.h"` `#include "hi_pwm.h"`

```c
#include <stdio.h>
#include <unistd.h>
#include "ohos_init.h"
#include "cmsis_os2.h"
#include "iot_gpio.h"
#include "iot_pwm.h"
#include "hi_io.h"
#include "hi_pwm.h"

// ███ 第4题多写：宏 ███
#define KEY_GPIO   5
#define BUZ_GPIO   9

// █████████ 骨架 █████████
static void PwmVolumeTask_cjh_hys(void *arg)
{
    (void)arg;

    // ███ 第4题多写：初始化 ███
    IoTGpioInit(KEY_GPIO);
    hi_io_set_func(KEY_GPIO, HI_IO_FUNC_GPIO_5_GPIO);
    IoTGpioSetDir(KEY_GPIO, IOT_GPIO_DIR_IN);

    IoTGpioInit(BUZ_GPIO);
    hi_io_set_func(BUZ_GPIO, HI_IO_FUNC_GPIO_9_PWM0_OUT);
    IoTGpioSetDir(BUZ_GPIO, IOT_GPIO_DIR_OUT);
    IoTPwmInit(0);

    // ███ 第4题多写：循环 ███
    while (1) {
        int val;
        IoTGpioGetInputVal(KEY_GPIO, &val);
        if (val == 0)  IoTPwmStart(0, 20, 4000);
        else           IoTPwmStop(0);
        osDelay(20);
    }
}

// ▼▼▼ 骨架不变 ▼▼▼
static void PwmVolumeEntry(void)
{
    osThreadAttr_t attr;
    attr.name = "PwmVolumeTask_cjh_hys";
    attr.stack_size = 4096;
    attr.priority = osPriorityNormal;
    osThreadNew(PwmVolumeTask_cjh_hys, NULL, &attr);
}
APP_FEATURE_INIT(PwmVolumeEntry);
```

---

# 第 5 题：交通灯

> 头文件：同第4题

```c
#include <stdio.h>
#include <unistd.h>
#include "ohos_init.h"
#include "cmsis_os2.h"
#include "iot_gpio.h"
#include "iot_pwm.h"
#include "hi_io.h"
#include "hi_pwm.h"

// ███ 第5题多写（全在骨架函数上面）███
#define LED_R   HI_IO_NAME_GPIO_10
#define LED_G   HI_IO_NAME_GPIO_11
#define LED_Y   HI_IO_NAME_GPIO_12
#define BUZ     HI_IO_NAME_GPIO_9

// █████████ 骨架 █████████
static void TrafficLightTask_cjh_hys(void *arg)
{
    (void)arg;

    // ███ 第5题多写：初始化 ███
    IoTGpioInit(LED_R); hi_io_set_func(LED_R, HI_IO_FUNC_GPIO_10_PWM1_OUT); IoTGpioSetDir(LED_R, IOT_GPIO_DIR_OUT);
    IoTGpioInit(LED_G); hi_io_set_func(LED_G, HI_IO_FUNC_GPIO_11_PWM2_OUT); IoTGpioSetDir(LED_G, IOT_GPIO_DIR_OUT);
    IoTGpioInit(LED_Y); hi_io_set_func(LED_Y, HI_IO_FUNC_GPIO_12_PWM3_OUT); IoTGpioSetDir(LED_Y, IOT_GPIO_DIR_OUT);
    IoTGpioInit(BUZ);   hi_io_set_func(BUZ,   HI_IO_FUNC_GPIO_9_PWM0_OUT);  IoTGpioSetDir(BUZ, IOT_GPIO_DIR_OUT);
    IoTPwmInit(0); IoTPwmInit(1); IoTPwmInit(2); IoTPwmInit(3);

    // ███ 第5题多写：循环 ███
    while (1) {
        IoTPwmStart(1,90,4000); IoTPwmStart(0,20,4000); osDelay(500); IoTPwmStop(0); osDelay(500);  // 红
        IoTPwmStart(2,90,4000); IoTPwmStart(0,20,4000); osDelay(200); IoTPwmStop(0); osDelay(200);  // 绿
        IoTPwmStart(3,90,4000); IoTPwmStart(0,20,4000); osDelay(500); IoTPwmStop(0); osDelay(500);  // 黄
    }
}

// ▼▼▼ 骨架不变 ▼▼▼
static void StartTrafficLightTask(void)
{
    osThreadAttr_t attr;
    attr.name = "TrafficLightTask_cjh_hys";
    attr.stack_size = 4096;
    attr.priority = osPriorityNormal;
    osThreadNew(TrafficLightTask_cjh_hys, NULL, &attr);
}
APP_FEATURE_INIT(StartTrafficLightTask);
```

---

# 第 6 题：炫彩灯

> 头文件：同第4题

```c
#include <stdio.h>
#include <unistd.h>
#include "ohos_init.h"
#include "cmsis_os2.h"
#include "iot_gpio.h"
#include "iot_pwm.h"
#include "hi_io.h"
#include "hi_pwm.h"

// █████████ 骨架 █████████
static void PwmLedTask_cjh_hys(void *arg)
{
    (void)arg;

    // ███ 第6题多写：初始化 ███
    IoTGpioInit(HI_IO_NAME_GPIO_10); hi_io_set_func(HI_IO_NAME_GPIO_10, HI_IO_FUNC_GPIO_10_PWM1_OUT); IoTGpioSetDir(HI_IO_NAME_GPIO_10, IOT_GPIO_DIR_OUT);
    IoTGpioInit(HI_IO_NAME_GPIO_11); hi_io_set_func(HI_IO_NAME_GPIO_11, HI_IO_FUNC_GPIO_11_PWM2_OUT); IoTGpioSetDir(HI_IO_NAME_GPIO_11, IOT_GPIO_DIR_OUT);
    IoTGpioInit(HI_IO_NAME_GPIO_12); hi_io_set_func(HI_IO_NAME_GPIO_12, HI_IO_FUNC_GPIO_12_PWM3_OUT); IoTGpioSetDir(HI_IO_NAME_GPIO_12, IOT_GPIO_DIR_OUT);
    IoTPwmInit(1); IoTPwmInit(2); IoTPwmInit(3);

    // ███ 第6题多写：循环 ███
    while (1) {
        for(int i=1;i<100;i++)
          for(int j=1;j<100;j++)
            for(int k=1;k<100;k++) {
                IoTPwmStart(1,i,4000); IoTPwmStart(2,j,4000); IoTPwmStart(3,k,4000);
                usleep(50000);
                IoTPwmStop(1); IoTPwmStop(2); IoTPwmStop(3);
            }
    }
}

// ▼▼▼ 骨架不变 ▼▼▼
static void PwmLedEntry(void)
{
    osThreadAttr_t attr;
    attr.name = "PwmLedTask_cjh_hys";
    attr.stack_size = 4096;
    attr.priority = osPriorityNormal;
    osThreadNew(PwmLedTask_cjh_hys, NULL, &attr);
}
APP_FEATURE_INIT(PwmLedEntry);
```

---

# 第 7 题：小夜灯

> 头文件：第4题的4个 + `#include "hi_adc.h"`

```c
#include <stdio.h>
#include <unistd.h>
#include "ohos_init.h"
#include "cmsis_os2.h"
#include "iot_gpio.h"
#include "iot_pwm.h"
#include "hi_io.h"
#include "hi_pwm.h"
#include "hi_adc.h"

// █████████ 骨架 █████████
static void NightLightTask_cjh_hys(void *arg)
{
    (void)arg;

    // ███ 第7题多写：初始化 ███
    IoTGpioInit(HI_IO_NAME_GPIO_10); hi_io_set_func(HI_IO_NAME_GPIO_10, HI_IO_FUNC_GPIO_10_PWM1_OUT); IoTGpioSetDir(HI_IO_NAME_GPIO_10, IOT_GPIO_DIR_OUT);
    IoTGpioInit(HI_IO_NAME_GPIO_11); hi_io_set_func(HI_IO_NAME_GPIO_11, HI_IO_FUNC_GPIO_11_PWM2_OUT); IoTGpioSetDir(HI_IO_NAME_GPIO_11, IOT_GPIO_DIR_OUT);
    IoTGpioInit(HI_IO_NAME_GPIO_12); hi_io_set_func(HI_IO_NAME_GPIO_12, HI_IO_FUNC_GPIO_12_PWM3_OUT); IoTGpioSetDir(HI_IO_NAME_GPIO_12, IOT_GPIO_DIR_OUT);
    IoTPwmInit(1); IoTPwmInit(2); IoTPwmInit(3);

    // ███ 第7题多写：循环 ███
    while (1) {
        unsigned short human=0, light=0;
        hi_adc_read(HI_ADC_CHANNEL_3, &human, HI_ADC_EQU_MODEL_4, HI_ADC_CUR_BAIS_DEFAULT, 0);
        hi_adc_read(HI_ADC_CHANNEL_4, &light, HI_ADC_EQU_MODEL_4, HI_ADC_CUR_BAIS_DEFAULT, 0);
        if (light>1200 && human>1200) {
            IoTPwmStart(1,65,4000); IoTPwmStart(2,39,4000); IoTPwmStart(3,8,4000);
        } else {
            IoTPwmStop(1); IoTPwmStop(2); IoTPwmStop(3);
        }
        osDelay(100);
    }
}

// ▼▼▼ 骨架不变 ▼▼▼
static void NightLightDemo(void)
{
    osThreadAttr_t attr;
    attr.name = "NightLightTask_cjh_hys";
    attr.stack_size = 4096;
    attr.priority = osPriorityNormal;
    osThreadNew(NightLightTask_cjh_hys, NULL, &attr);
}
APP_FEATURE_INIT(NightLightDemo);
```

---

# 第 8 题：燃气报警

> 头文件：第4题的4个 + `#include "hi_adc.h"`

```c
#include <stdio.h>
#include <unistd.h>
#include "ohos_init.h"
#include "cmsis_os2.h"
#include "iot_gpio.h"
#include "iot_pwm.h"
#include "hi_io.h"
#include "hi_pwm.h"
#include "hi_adc.h"

// █████████ 骨架 █████████
static void ADCGasTask_cjh_hys(void *arg)
{
    (void)arg;

    // ███ 第8题多写：初始化 ███
    IoTGpioInit(HI_IO_NAME_GPIO_9);
    hi_io_set_func(HI_IO_NAME_GPIO_9, HI_IO_FUNC_GPIO_9_PWM0_OUT);
    IoTGpioSetDir(HI_IO_NAME_GPIO_9, IOT_GPIO_DIR_OUT);
    IoTPwmInit(0);

    // ███ 第8题多写：循环 ███
    while (1) {
        unsigned short data=0;
        hi_adc_read(HI_ADC_CHANNEL_5, &data, HI_ADC_EQU_MODEL_4, HI_ADC_CUR_BAIS_DEFAULT, 0);
        if (data > 600)  IoTPwmStart(0, 50, 2700);
        else             IoTPwmStop(0);
        osDelay(10);
    }
}

// ▼▼▼ 骨架不变 ▼▼▼
static void ADCGasDemo(void)
{
    osThreadAttr_t attr;
    attr.name = "ADCGasTask_cjh_hys";
    attr.stack_size = 4096;
    attr.priority = osPriorityNormal;
    osThreadNew(ADCGasTask_cjh_hys, NULL, &attr);
}
APP_FEATURE_INIT(ADCGasDemo);
```

---

# 第 9 题：温湿度

> 头文件：第4题的4个 + `#include "iot_i2c.h"` `#include "aht20.h"`

```c
#include <stdio.h>
#include <unistd.h>
#include "ohos_init.h"
#include "cmsis_os2.h"
#include "iot_gpio.h"
#include "iot_pwm.h"
#include "hi_io.h"
#include "hi_pwm.h"
#include "iot_i2c.h"
#include "aht20.h"

// █████████ 骨架 █████████
static void tempHumiTask_cjh_hys(void *arg)
{
    (void)arg;

    // ███ 第9题多写：初始化 ███
    IoTGpioInit(HI_IO_NAME_GPIO_13);
    hi_io_set_func(HI_IO_NAME_GPIO_13, HI_IO_FUNC_GPIO_13_I2C0_SDA);
    IoTGpioInit(HI_IO_NAME_GPIO_14);
    hi_io_set_func(HI_IO_NAME_GPIO_14, HI_IO_FUNC_GPIO_14_I2C0_SCL);
    IoTI2cInit(HI_I2C_IDX_0, 400000);
    AHT20_Calibrate();

    IoTGpioInit(HI_IO_NAME_GPIO_9);
    hi_io_set_func(HI_IO_NAME_GPIO_9, HI_IO_FUNC_GPIO_9_PWM0_OUT);
    IoTGpioSetDir(HI_IO_NAME_GPIO_9, IOT_GPIO_DIR_OUT);
    IoTPwmInit(0);

    // ███ 第9题多写：循环 ███
    while (1) {
        float t, h;
        AHT20_StartMeasure();
        AHT20_GetMeasureResult(&t, &h);
        if (h > 90)  IoTPwmStart(0, 50, 2700);
        else         IoTPwmStop(0);
        osDelay(100);
    }
}

// ▼▼▼ 骨架不变 ▼▼▼
static void tempHumiDemo(void)
{
    osThreadAttr_t attr;
    attr.name = "tempHumiTask_cjh_hys";
    attr.stack_size = 4096;
    attr.priority = osPriorityNormal;
    osThreadNew(tempHumiTask_cjh_hys, NULL, &attr);
}
APP_FEATURE_INIT(tempHumiDemo);
```

---

# 第 10 题：OLED 滚动

> 头文件：基础4个 + `#include "ssd1306.h"` `#include <string.h>`

```c
#include <stdio.h>
#include <unistd.h>
#include <string.h>
#include "ohos_init.h"
#include "cmsis_os2.h"
#include "ssd1306.h"

// ███ 第10题多写：全局 ███
#define STU_ID   "你的学号"
#define NAME_PY  "liming"
char ad_buf[256];
int str_len;

// █████████ 骨架 █████████
static void ScrollDisplayTask(void *arg)
{
    (void)arg;

    // ███ 第10题多写：初始化 ███
    ssd1306_Init();
    sprintf(ad_buf, "%s%Sabcdefghijklmnopqrstuvwxyz", STU_ID, NAME_PY);
    str_len = strlen(ad_buf);

    // ███ 第10题多写：循环 ███
    int offset = 0;
    while (1) {
        char show[17] = {0};
        for (int i=0; i<16; i++) show[i] = ad_buf[(offset+i) % str_len];
        ssd1306_Fill(Black);
        ssd1306_SetCursor(0, 20);
        ssd1306_DrawString(show, Font_7x10, White);
        ssd1306_UpdateScreen();
        offset++;
        osDelay(500);
    }
}

// ▼▼▼ 骨架不变 ▼▼▼
static void Ssd1306ScrollDemo(void)
{
    osThreadAttr_t attr;
    attr.name = "ScrollDisplayTask";
    attr.stack_size = 4096;
    attr.priority = osPriorityNormal;
    osThreadNew(ScrollDisplayTask, NULL, &attr);
}
APP_FEATURE_INIT(Ssd1306ScrollDemo);
```

---

# 第 11 题：按键移字符

> 头文件同第10题

```c
#include <stdio.h>
#include <unistd.h>
#include "ohos_init.h"
#include "cmsis_os2.h"
#include "ssd1306.h"

// █████████ 骨架 █████████
static void 这里换名字_cjh_hys(void *arg)
{
    (void)arg;

    // ███ 第11题多写：初始化 ███
    ssd1306_Init();
    int x = 64;

    // ███ 第11题多写：循环 ███
    while (1) {
        // 读S1/S2按键（具体读键代码看题目给的）
        if (/*按了S1*/ && x>0)    x -= 7;
        if (/*按了S2*/ && x<121)  x += 7;
        ssd1306_Fill(Black);
        ssd1306_SetCursor(x, 30);
        ssd1306_DrawChar('x', Font_7x10, White);
        ssd1306_UpdateScreen();
        osDelay(50);
    }
}

// ▼▼▼ 骨架不变 ▼▼▼
static void 这里换名字(void)
{
    osThreadAttr_t attr;
    attr.name = "这里换名字_cjh_hys";
    attr.stack_size = 4096;
    attr.priority = osPriorityNormal;
    osThreadNew(这里换名字_cjh_hys, NULL, &attr);
}
APP_FEATURE_INIT(这里换名字);
```

---

# 第 12 题：交通灯倒计时

> 头文件同第10题

```c
#include <stdio.h>
#include <unistd.h>
#include "ohos_init.h"
#include "cmsis_os2.h"
#include "ssd1306.h"

// █████████ 骨架 █████████
static void 这里换名字_cjh_hys(void *arg)
{
    (void)arg;

    // ███ 第12题多写：初始化 ███
    ssd1306_Init();

    // ███ 第12题多写：循环 ███
    int state = 0;   // 0=绿 1=黄 2=红
    while (1) {
        int sec = (state==0) ? 20 : (state==1) ? 5 : 15;

        while (sec > 0) {
            char buf[32];
            ssd1306_Fill(Black);
            ssd1306_SetCursor(20, 20);
            ssd1306_DrawString((state==0)?"Green":(state==1)?"Yellow":"Red", Font_7x10, White);
            snprintf(buf, sizeof(buf), "Time: %02d", sec);
            ssd1306_SetCursor(20, 45);
            ssd1306_DrawString(buf, Font_7x10, White);
            ssd1306_UpdateScreen();
            osDelay(1000);
            sec--;
        }
        state = (state + 1) % 3;
    }
}

// ▼▼▼ 骨架不变 ▼▼▼
static void 这里换名字(void)
{
    osThreadAttr_t attr;
    attr.name = "这里换名字_cjh_hys";
    attr.stack_size = 4096;
    attr.priority = osPriorityNormal;
    osThreadNew(这里换名字_cjh_hys, NULL, &attr);
}
APP_FEATURE_INIT(这里换名字);
```

---

# 就这一份，完了。

``` 
每道题 = 骨架（12题一样的） + ███ 框起来的几行（各题不同）
                                         ↑
                                   你只需要背这些███
```
